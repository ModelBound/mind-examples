# typescript-coding-assistant

A coding assistant tuned for a small TypeScript team. Remembers per-user style preferences and proposes memory updates instead of overwriting.

Copy `.mind/` into your project root, then point any MCP-capable agent at it via [`@modelbound/mind-mcp`](https://github.com/ModelBound/mind-mcp).
